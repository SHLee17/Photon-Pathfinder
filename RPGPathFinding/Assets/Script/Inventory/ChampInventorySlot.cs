using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChampInventorySlot : StorageManager
{
    //public void AddChamp(Champion _champ, EPlayerSide ePlayerSide = EPlayerSide.player)
    //{
    //    champ = Instantiate(_champ);
    //    champ.ePlayerSide = ePlayerSide;
    //    champ.LastPos(transform.position);
    //}
}
